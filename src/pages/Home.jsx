import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div className="flex flex-col">
      <Typography variant="h1">Portafolio</Typography>
      <Typography variant="h2">Grupo FROX</Typography>
    </div>
  );
};

export default Home;
